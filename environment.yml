name: cipher
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - python-dotenv>=1.0.0
      - pydantic>=2.5.0
      - pydantic-settings>=2.2.0
      - networkx>=3.2
      - openai>=1.30.0
      - rich>=13.7.0
      - colorlog>=6.8.0
      - requests>=2.31.0
      - pytest>=8.0.0
      - pytest-asyncio>=0.23.0
      - plotly>=5.20.0
      - dash>=2.16.0
      - dash-bootstrap-components>=1.5.0
      - flask>=3.0.0
      - flask-cors>=5.0.0
      - numpy>=1.26.0
      - pandas>=2.0.0
      - matplotlib>=3.8.0
      - openenv>=0.1.13
      - huggingface_hub>=0.23.0
      - gunicorn>=22.0.0
      # LoRA hybrid: install PyTorch from conda-forge/pytorch, then `pip install -r requirements.txt`
