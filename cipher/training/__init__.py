"""
CIPHER training package.

Contains the training loop, episode runner, and self-play infrastructure.

This package does NOT contain agent implementations, reward functions, or
environment definitions — it orchestrates all of them.
"""
from __future__ import annotations
