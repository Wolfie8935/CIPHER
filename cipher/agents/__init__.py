"""
CIPHER agents package.

Contains the abstract base agent, all RED team agents, all BLUE team agents,
and the prompt templates used by each agent.

This package does NOT contain the training loop, reward functions, or environment logic.
"""
from __future__ import annotations
