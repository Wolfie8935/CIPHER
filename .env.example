# CIPHER — copy to .env and set your Hugging Face Inference credentials.
# All other settings (models, LoRA paths, LLM_MODE defaults, etc.) are in cipher/utils/config.py

HF_TOKEN=
HF_BASE_URL=https://router.huggingface.co/v1
